node_modules
dist
.vercel
.DS_Store
*.local
.env

# 事業計画書などのバイナリ資料（リポジトリでは管理しない）
*.docx
*.pptx

# DB認証情報を含むローカル用スクリプト
apply_migrations.command

# Claude Code のローカル設定（DBパスワード等が入るため必ず除外）
.claude/settings.local.json
